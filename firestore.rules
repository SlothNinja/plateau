rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /Committed/{gid}/View/{uid}/{document=**} {
      allow read: if request.auth.uid == uid;
    }
  }
}
